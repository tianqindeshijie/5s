spring:
  application:
    name: micro-service-mysql
#  通过指定配置服务器地址获取配置信息
#  cloud:
#    config:
#      uri: http://localhost:8888
#      fail-fast: true
# 通过服务发现得到配置信息
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        service-id: configserver
eureka:
  client:
    serviceUrl:
      defaultZone: http://172.19.3.138:8761/eureka/
  instance:
    prefer-ip-address: true